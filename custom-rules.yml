rules:
  - id: missing-output-encoding
    patterns:
      - pattern: |
          print($DATA)
    message: "Potential XSS: Output is printed without encoding. Use html.escape()."
    severity: ERROR
    languages:
      - python
    metadata:
      category: security
      technology: web
